# Code Mode - Style Analyzer Pro

**See main [AGENTS.md](../../AGENTS.md) for complete project rules, build commands, and coding standards.**

## Code Mode Specifics

When implementing features or fixes:

- Run commands from `wxt-version/` directory
- Test with `npm run test:logic` before committing
- Type check with `npm run compile` before building
